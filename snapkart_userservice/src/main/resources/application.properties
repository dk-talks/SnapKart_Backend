spring.application.name=SnapKartUserService
# MySQL Database Configuration
spring.datasource.url=${MYSQL_USER_DATABASE_URL}
spring.datasource.username=${MYSQL_USER_DATABASE_USER}
spring.datasource.password=${MYSQL_USER_DATABASE_PASSWORD}

# JPA/Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=8080
eureka.instance.prefer-ip-address=true
#eureka.client.service-url.defaultZone=http://snapkart_eurekaserver:8761/eureka
#eureka.client.fetch-registry=true
#eureka.client.register-with-eureka=true

